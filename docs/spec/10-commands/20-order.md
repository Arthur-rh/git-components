# Subcommand Specification: order

## Status

**Not part of v1.** This subcommand is planned for a later release (see `04-cli.md`, "Subcommands"). It **shall not** be implemented, documented as available, or relied upon by v1 of `git-component`.

***TBD*** — full specification deferred to the release that introduces this subcommand.
